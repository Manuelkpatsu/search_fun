import 'package:flutter/material.dart';

class SelectFilterText extends StatelessWidget {
  const SelectFilterText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Select Filters'
    );
  }
}
